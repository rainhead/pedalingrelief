# Development dependencies for MacOS.
# First install Homebrew: https://docs.brew.sh/Installation
# Then run `brew bundle` to install the dependencies below.

tap 'homebrew/core'
tap 'homebrew/bundle'
tap 'homebrew/services'
tap 'homebrew/cask'

brew 'heroku'
brew 'postgis'
brew 'postgresql'
brew 'pyenv'
